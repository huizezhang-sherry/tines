% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen-code.R
\name{gen_code}
\alias{gen_code}
\alias{gen_code.character}
\alias{gen_code.schema}
\alias{gen_code.multiverse}
\alias{prompt_gen_code}
\title{Generate R code from a schema or multiverse}
\usage{
gen_code(
  x,
  base_code = NULL,
  data = NULL,
  output = "scripts",
  provider = "gemini",
  ...
)

\method{gen_code}{character}(
  x,
  base_code = NULL,
  data = NULL,
  output = "scripts",
  provider = "gemini",
  ...
)

\method{gen_code}{schema}(
  x,
  base_code = NULL,
  data = NULL,
  output = "scripts",
  provider = "gemini",
  ...
)

\method{gen_code}{multiverse}(
  x,
  base_code = NULL,
  data = NULL,
  output = "scripts",
  provider = "gemini",
  ...
)

prompt_gen_code(
  schema = NULL,
  base_code = NULL,
  data = NULL,
  print = TRUE,
  width = 70
)
}
\arguments{
\item{x}{A schema object, multiverse object, or file path to a schema YML.}

\item{base_code}{Optional. File path to an R script to use as style reference.}

\item{data}{Optional. Data source specification (file path or "package::dataset").}

\item{output}{For schemas: file path (ending in .R) or directory. 
For multiverses: must be a directory. Defaults to "scripts".}

\item{provider}{The LLM provider to use for code generation. Currently only "gemini".}

\item{...}{Additional arguments passed to methods.}

\item{schema}{A schema object to include in the prompt.}

\item{print}{If `TRUE`, prints the prompt to console instead of returning it.}

\item{width}{If `print = TRUE`, the width to wrap the printed prompt (default 70).}
}
\value{
Invisibly returns the path(s) to the generated script(s).
}
\description{
Generate R code from a schema or multiverse
}
\examples{
\dontrun{
# Generate from a single schema
schema <- example_schema()
gen_code(schema, output = "analysis.R")

# Generate from a multiverse (use expand_tines first)
schema |>
  expand_tines(alternatives) |>
  gen_code(output = "scripts/")
}

# The prompt generation function can be used directly to see the full prompt sent to the LLM
prompt_gen_code(data = "inst/football.csv")
}
