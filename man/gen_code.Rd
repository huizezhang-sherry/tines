% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen-code.R
\name{gen_code}
\alias{gen_code}
\alias{gen_code.character}
\alias{gen_code.schema}
\alias{gen_code.multiverse}
\alias{prompt_gen_code}
\title{Generate R code from a schema or multiverse}
\usage{
gen_code(
  x,
  base_code = NULL,
  data = NULL,
  output = "scripts",
  model = "anthropic/claude-opus-4-5",
  ...
)

\method{gen_code}{character}(
  x,
  base_code = NULL,
  data = NULL,
  output = "scripts",
  model = "anthropic/claude-opus-4-5",
  ...
)

\method{gen_code}{schema}(
  x,
  base_code = NULL,
  data = NULL,
  output = "scripts",
  model = "anthropic/claude-opus-4-5",
  ...
)

\method{gen_code}{multiverse}(
  x,
  base_code = NULL,
  data = NULL,
  output = "scripts",
  model = "anthropic/claude-opus-4-5",
  ...
)

prompt_gen_code(
  schema = NULL,
  base_code = NULL,
  data = NULL,
  print = TRUE,
  width = 70
)
}
\arguments{
\item{x}{A schema object, multiverse object, or file path to a schema YML.}

\item{base_code}{Optional. File path to an R script to use as style
reference.}

\item{data}{Optional. Data source specification (file path or
"package::dataset").}

\item{output}{For schemas: file path (ending in .R) or directory.
For multiverses: must be a directory. Defaults to "scripts".}

\item{model}{The LLM to use for code generation, as a string in
\code{"provider/model"} form (e.g. \code{"anthropic/claude-opus-4-5"},
\code{"openai/gpt-5"}, \code{"google_gemini/gemini-2.5-flash"}), passed to
\code{ellmer::chat()}. See \code{\link[ellmer:chat]{ellmer::chat()}} for the full list of supported
providers. Defaults to \code{"anthropic/claude-opus-4-5"}.}

\item{...}{Additional arguments passed to methods.}

\item{schema}{A schema object to include in the prompt.}

\item{print}{If \code{TRUE}, prints the prompt to console instead of returning
it.}

\item{width}{If \code{print = TRUE}, the width to wrap the printed prompt
(default 70).}
}
\value{
Invisibly returns the path(s) to the generated script(s).
}
\description{
Generate R code from a schema or multiverse
}
\examples{
\dontrun{
# Requires an LLM API key (e.g. ANTHROPIC_API_KEY); not run automatically.
# Generate from a single schema
schema <- example_schema()
gen_code(schema, output = "analysis.R")

# Generate from a multiverse (use expand_tines first)
schema |>
  expand_tines(alternatives) |>
  gen_code(output = "scripts/")
}

# The prompt generation function can be used directly to see the full
# prompt sent to the LLM
prompt_gen_code(data = "inst/football.csv")
}
