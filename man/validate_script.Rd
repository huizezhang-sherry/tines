% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_script.R
\name{validate_script}
\alias{validate_script}
\title{Auto-Fix an R Script via Iterative LLM Debugging}
\usage{
validate_script(
  file,
  data = NULL,
  max_runs = 5,
  model = "gemini-2.5-pro",
  verbose = TRUE,
  as_job = FALSE,
  engine = c("callr", "docker"),
  scan_code = TRUE
)
}
\arguments{
\item{file}{Path to the R script to run and debug.}

\item{data}{Optional path to a CSV data file to make available inside
the sandbox at \code{data/<basename>}.}

\item{max_runs}{Maximum number of LLM fix attempts. Default is \code{5}.}

\item{model}{Gemini model string. Default is \code{"gemini-2.5-pro"}.}

\item{verbose}{If \code{TRUE}, saves a copy of the script at each iteration.}

\item{as_job}{If \code{TRUE}, runs the process as a background job. If
\code{FALSE} (default), runs in the current R session. Only used when \code{engine = "callr"}.}

\item{engine}{One of \code{"callr"} (default) or \code{"docker"}. Controls
the execution to be with \code{callr} in a temp directory sandbox (in session or as a background job),
or within a Docker container.}

\item{scan_code}{If \code{TRUE} (default), scans the script for potentially
dangerous file system commands before execution.}
}
\value{
Invisibly returns a list with:
  \describe{
    \item{success}{Logical. Whether the script ran without error.}
    \item{file}{Path to the (possibly fixed) script.}
    \item{chat_history}{The \code{ellmer} chat object with full history.}
  }
}
\description{
Executes an R script in an isolated sandbox. If execution fails, the error
is passed to an LLM (currently only Google Gemini) which attempts to fix the
script. This loop repeats up to \code{max_runs} times.
}
\examples{
\dontrun{
validate_script(
  file = here::here("test/dplyr-filter-equal.R"),
  data = here::here("data/dplyr-filter-equal.csv")
)
}

}
