% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructor.R
\name{new_multiverse}
\alias{new_multiverse}
\alias{as_multiverse}
\alias{as_multiverse.default}
\alias{as_multiverse.multiverse}
\alias{as_multiverse.schema}
\alias{as_multiverse.list}
\title{Construct a multiverse}
\usage{
new_multiverse(schemas = list())

as_multiverse(x, ...)

\method{as_multiverse}{default}(x, ...)

\method{as_multiverse}{multiverse}(x, ...)

\method{as_multiverse}{schema}(x, ...)

\method{as_multiverse}{list}(x, ...)
}
\arguments{
\item{schemas}{A list of \code{schema} objects.}

\item{x}{A \code{schema}, a \code{multiverse}, or a list of either to be coerced.}

\item{...}{Passed on to methods.}
}
\value{
An object of class \code{c("multiverse", "list")}.
}
\description{
A multiverse is a collection of related schemas. It is never authored from
scratch: \code{\link[=as_multiverse]{as_multiverse()}} collects schemas you already have, and
\code{\link[=expand_tines]{expand_tines()}} derives one from a schema and an alternatives file.
\code{\link[=new_multiverse]{new_multiverse()}} is the low-level constructor.
}
\details{
Branches follow list conventions -- they may be named or not, exactly as
the list you pass in leaves them.
}
\examples{
s1 <- example_schema()
s2 <- example_football()

as_multiverse(list(hdi = s1, football = s2))

# names are optional, as in any list
as_multiverse(list(s1, s2))

}
\seealso{
\code{\link[=build_schema]{build_schema()}} to make the schemas in the first place.
}
