% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_block.R
\name{import_block}
\alias{import_block}
\alias{generate_edges}
\title{Import a block from a source schema into the current schema}
\usage{
import_block(schema, source_schema, tag, source_schema_name = NULL, ...)

generate_edges(schema)
}
\arguments{
\item{schema}{A schema object with nodes containing `inputs` and `outputs`
list-columns.}

\item{source_schema}{The schema to import the block from.}

\item{tag}{The tag of the block to import.}

\item{source_schema_name}{Optional. A string to use as the provenance key,
matched against `base_scripts` in `gen_composite_code()`. Defaults to
the deparsed name of `source_schema`.}

\item{...}{Optional field overrides (e.g. `inputs = c(".new_var")`).}
}
\value{
The updated schema with the imported block appended.

The schema with `edges` populated.
}
\description{
Import a block from a source schema into the current schema

Generate edges from a schema based on input/output variable matching
}
