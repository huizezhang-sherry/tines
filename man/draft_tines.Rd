% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create-template.R
\name{draft_tines}
\alias{draft_tines}
\title{Draft a schema template}
\usage{
draft_tines(file_path = NULL, overwrite = FALSE)
}
\arguments{
\item{file_path}{Where to save the template. If \code{NULL}, it is written to
the working directory as \code{schema_template.yml}.}

\item{overwrite}{Logical. If \code{TRUE}, overwrites an existing file at
\code{file_path}. Defaults to \code{FALSE}.}
}
\value{
Invisibly, the path written to; called for its side effect of
writing the file.
}
\description{
Writes a starter YAML file with two placeholder steps, ready to fill in by
hand. There is no equivalent for a multiverse: a multiverse is always
derived, either by collecting schemas with \code{\link[=as_multiverse]{as_multiverse()}} or by
expanding one with an alternatives file via \code{\link[=expand_tines]{expand_tines()}}.
}
\examples{
schema_path <- withr::local_tempfile(fileext = ".yml")
draft_tines(file_path = schema_path)

}
\seealso{
\code{\link[=draft_alternatives]{draft_alternatives()}} for the alternatives-file equivalent, and
\code{\link[=read_tines]{read_tines()}} to read the filled-in template back.
}
