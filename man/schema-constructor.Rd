% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructor.R
\name{new_schema}
\alias{new_schema}
\alias{build_schema}
\alias{add_step}
\alias{as_schema}
\alias{as_schema.default}
\alias{as_schema.schema}
\alias{as_schema.data.frame}
\alias{as.data.frame.schema}
\alias{print.schema}
\title{Construct a schema}
\usage{
new_schema(name = NULL, nodes = tibble::tibble())

build_schema(name = NULL, data = NULL)

add_step(
  object,
  id,
  objective = "",
  decision = "",
  rationale = "",
  inputs = NULL,
  outputs = NULL,
  ...
)

as_schema(x, ...)

\method{as_schema}{default}(x, ...)

\method{as_schema}{schema}(x, ...)

\method{as_schema}{data.frame}(x, name = NULL, ...)

\method{as.data.frame}{schema}(x, row.names = NULL, optional = FALSE, ...)

\method{print}{schema}(x, width = NULL, ...)
}
\arguments{
\item{name}{An optional name for the schema.}

\item{nodes}{A data frame (typically a \code{tibble}) defining the steps of the
schema.}

\item{data}{Optional data frame or path to data file for validation}

\item{object}{A \code{schema} object.}

\item{id, objective, decision, rationale, inputs, outputs}{character strings
to write a step}

\item{...}{Passed on to methods.}

\item{x}{An object to be coerced into a \code{schema}.}

\item{row.names}{NULL or a character vector giving the row names for the
data frame.}

\item{optional}{logical. If TRUE, setting row names and converting column
names is optional.}

\item{width}{Width for printing output.}
}
\value{
An object of class \code{schema}: a tibble with one row per step.
}
\description{
A schema records one analysis as an ordered sequence of decisions.
\code{\link[=build_schema]{build_schema()}} starts an empty one and \code{\link[=add_step]{add_step()}} appends a decision to
it; \code{\link[=as_schema]{as_schema()}} coerces an existing table of steps; \code{\link[=new_schema]{new_schema()}} is the
low-level constructor the others are built on.
}
\examples{
schema <- build_schema("HDI Example") |>
  add_step(
    id = "step-scaling",
    objective = "variables are in different scales",
    decision = "apply min-max scaling to each variable",
    rationale = "to put them on the same scale for combination"
  ) |>
  add_step(
    id = "step-combine",
    objective = "combine the three dimensions into a single index",
    decision = "use the geometric mean",
    rationale = "the geometric mean penalizes uneven development"
  )

schema

# coerce a table of steps that was catalogued elsewhere
as_schema(data.frame(
  id = "step-clean", objective = "handle missing values",
  decision = "drop incomplete cases", rationale = "keeps it simple"
))

}
\seealso{
\code{\link[=as_multiverse]{as_multiverse()}} to collect several schemas together, and
\code{\link[=expand_tines]{expand_tines()}} to expand one into a multiverse.
}
