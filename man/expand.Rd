% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen-alternatives.R
\name{expand_tines}
\alias{expand_tines}
\alias{expand_tines.schema}
\alias{expand_tines.multiverse}
\title{Expand a schema with an alternative YAML into a multiverse}
\usage{
expand_tines(x, alternatives, ...)

\method{expand_tines}{schema}(x, alternatives, include_original = TRUE, ...)

\method{expand_tines}{multiverse}(x, alternatives, ...)
}
\arguments{
\item{x}{A `schema` object.}

\item{alternatives}{an `alternatives` object or the path to an alternative YAML}

\item{...}{Additional arguments.}

\item{include_original}{A logical. If `TRUE`, the original schema will be included as a branch in the resulting multiverse. Defaults to `TRUE`.}
}
\description{
Expand a schema with an alternative YAML into a multiverse
}
\examples{

# expand on a schema
base_schema <- example_football()
alts <- example_alternatives(case = "football")
expand_tines(base_schema, alts)

# read the alternatives from a YML file
tmp_file <- tempfile(fileext = ".yml")
write_alternatives(alts, tmp_file)
expand_tines(base_schema, tmp_file)

# expand on the multiverse
multiverse <- example_multiverse()
alts <- example_alternatives(case = "hdi")
expand_tines(multiverse, alts)

}
