% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create-template.R
\name{draft_alternatives}
\alias{draft_alternatives}
\title{Draft an alternatives template}
\usage{
draft_alternatives(x, id, file_path = NULL, branch)
}
\arguments{
\item{x}{A \code{schema} object, or the path to a schema YAML file.}

\item{id}{The \code{id} of the step to draft alternatives for; a vector names
several, and one node is drafted per step.}

\item{file_path}{Where to save the template. If \code{NULL}, it is written to
the working directory, named after the steps it covers.}

\item{branch}{Required: either \code{"multi"} (drafts 2 placeholder
alternatives per node) or \code{"single"} (drafts exactly 1 per node). See
\code{vignette("alternatives")} for what the two modes mean when expanded.}
}
\value{
Invisibly, the path written to; called for its side effect of
writing the file.
}
\description{
Writes a starter alternatives YAML file, with one node per step named in
\code{id} and placeholder alternatives inside each, ready to fill in by hand.
}
\examples{
my_schema <- example_hdi()

draft_alternatives(
  x = my_schema,
  id = "step-scaling",
  file_path = withr::local_tempfile(fileext = ".yml"),
  branch = "multi"
)

# `x` also accepts a path to a schema file -- draft a single-branch
# template combining two steps together
draft_alternatives(
  x = system.file("hdi.yml", package = "tines"),
  id = c("step-scaling", "step-education"),
  file_path = withr::local_tempfile(fileext = ".yml"),
  branch = "single"
)

}
\seealso{
\code{\link[=gen_alternatives]{gen_alternatives()}} to have an LLM propose them instead, and
\code{\link[=read_alternatives]{read_alternatives()}} to read the filled-in file back.
}
