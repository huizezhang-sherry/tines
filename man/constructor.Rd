% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructor.R
\name{new_schema}
\alias{new_schema}
\alias{build_schema}
\alias{new_multiverse}
\alias{build_multiverse}
\alias{add_step}
\alias{as_schema}
\alias{as_schema.default}
\alias{as_schema.schema}
\alias{as_schema.list}
\alias{as_schema.character}
\alias{as_multiverse}
\alias{as_multiverse.default}
\alias{as_multiverse.multiverse}
\alias{as_multiverse.schema}
\alias{as_multiverse.list}
\alias{c.schema}
\alias{c.multiverse}
\alias{as.data.frame.schema}
\alias{print.schema}
\title{Construct schema and multiverse objects}
\usage{
new_schema(name = NULL, nodes = tibble::tibble())

build_schema(name = NULL, data = NULL)

new_multiverse(schemas = list())

build_multiverse(...)

add_step(
  object,
  id,
  objective = "",
  decision = "",
  rationale = "",
  inputs = NULL,
  outputs = NULL,
  ...
)

as_schema(x, ...)

\method{as_schema}{default}(x, ...)

\method{as_schema}{schema}(x, ...)

\method{as_schema}{list}(x, ...)

\method{as_schema}{character}(x, ...)

as_multiverse(x, ...)

\method{as_multiverse}{default}(x, ...)

\method{as_multiverse}{multiverse}(x, ...)

\method{as_multiverse}{schema}(x, ...)

\method{as_multiverse}{list}(x, ...)

\method{c}{schema}(...)

\method{c}{multiverse}(...)

\method{as.data.frame}{schema}(x, row.names = NULL, optional = FALSE, ...)

\method{print}{schema}(x, width = NULL, ...)
}
\arguments{
\item{name}{An optional name for the schema.}

\item{nodes}{A data frame (typically a \code{tibble}) defining the steps of the
schema.}

\item{data}{Optional data frame or path to data file for validation}

\item{...}{One or more \code{schema} objects to be included in the multiverse.}

\item{object}{A \code{schema} object.}

\item{id, objective, decision, rationale, inputs, outputs}{character strings
to write a step}

\item{x}{An object to be coerced into a \code{schema} or \code{multiverse}.}

\item{row.names}{NULL or a character vector giving the row names for the
data frame.}

\item{optional}{logical. If TRUE, setting row names and converting column
names is optional.}

\item{width}{Width for printing output.}

\item{schema, schemas}{A single list containing objects of class \code{schema}.
Defaults to an empty list.}
}
\value{
\itemize{
\item \code{\link[=build_schema]{build_schema()}} and \code{\link[=new_schema]{new_schema()}} return an object of class \code{schema}.
\item \code{\link[=build_multiverse]{build_multiverse()}} and \code{\link[=new_multiverse]{new_multiverse()}} return an object of class
\code{c("multiverse", "list")}.
}
}
\description{
Construct individual analytical paths (\code{schema}) and bundle them into a
garden of forking paths (\code{multiverse}).
}
\examples{
schema <- build_schema("HDI Example") |>
  # 1. The Scaling step
  add_step(
    id = "step-scaling",
    objective = "variables are in different scales",
    decision = "apply min-max scaling to each variable",
    rationale = "to put them on the same scale for combination"
  ) |>
  # 2. The Education step
  add_step(
    id = "step-education",
    objective = "combine the school variables into one dimension",
    decision = "average exp sch and avg sch",
    rationale = "the most intuitive way"
  ) |>
  # 3. The Combine step
  add_step(
    id = "step-combine",
    objective = "combine the three dimensions into a single index",
    decision = "use the geometric mean",
    rationale = "the geometric mean is more appropriate than arithmetic mean"
  )

schema

schema2 <- build_schema("HDI Example") |>
  # 1. The Education Step
  add_step(
    id = "step-education",
    objective = "combine the school variables into one dimension",
    decision = "average exp sch and avg sch",
    rationale = "the most intuitive way"
  ) |>
  # 2. The Scaling Step
  add_step(
    id = "step-scaling",
    objective = "variables are in different scales",
    decision = "apply min-max scaling to each variable",
    rationale = "to put them on the same scale for combination"
  ) |>
  # 3. The Combine Step
  add_step(
    id = "step-combine",
    objective = "combine the three dimensions into a single index",
    decision = "use the geometric mean",
    rationale = "the geometric mean is more appropriate than arithmetic mean"
  )

my_multiverse <- build_multiverse(original = schema, reversed = schema2)
my_multiverse
}
