% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alternatives.R
\name{alternative}
\alias{alternative}
\alias{node}
\alias{new_alternatives}
\title{Construct alternatives objects}
\usage{
alternative(id, decision, rationale)

node(overrides, ...)

new_alternatives(..., branch)
}
\arguments{
\item{id}{Unique identifier for this alternative (kebab-case).}

\item{decision}{The new method/implementation.}

\item{rationale}{Why this method is valid.}

\item{overrides}{The id of the step this node overrides.}

\item{...}{For \code{\link[=node]{node()}}: one or more alternatives, created by
\code{\link[=alternative]{alternative()}}. For \code{\link[=new_alternatives]{new_alternatives()}}: one or more nodes, created by
\code{\link[=node]{node()}}.}

\item{branch}{Required: either \code{"multi"} or \code{"single"}. There is no
default -- the two modes produce very different numbers of branches, so
this must be chosen explicitly.

\code{"multi"} treats each node's alternatives as independent choices:
\code{\link[=expand_tines]{expand_tines()}} expands into the full cross product of "keep this
step's original decision" plus each listed alternative, across every
node. A single node with N alternatives is just N new branches (as
today); K nodes with \code{n_i} alternatives each give the full
\code{prod(n_i + 1)} factorial, since "keep original" is itself one of the
choices at every node.

\code{"single"} requires exactly one alternative per node, and combines all
nodes' (sole) alternatives into a single coordinated branch -- useful
for expressing one change that spans several steps together.}
}
\value{
\code{\link[=alternative]{alternative()}} returns a plain list with elements \code{id},
\code{decision}, \code{rationale} -- one candidate change for a single step.

\code{\link[=node]{node()}} returns a plain list with elements \code{overrides} (the step id)
and \code{alternatives} (a tibble of the candidates passed via \code{...}, one
row per \code{\link[=alternative]{alternative()}}).

\code{\link[=new_alternatives]{new_alternatives()}} returns an object of class \code{"alternatives"} (a
tibble subclass with columns \code{overrides} and \code{alternatives}, the
latter a list-column of per-node tibbles as built by \code{\link[=node]{node()}}), with
the chosen \code{branch} mode stored as an attribute. This is the object
consumed by \code{\link[=expand_tines]{expand_tines()}} and \code{\link[=write_alternatives]{write_alternatives()}}.
}
\description{
Construct alternatives objects
}
\examples{
example_football_grp5_alternatives()

# multi-branch: 3 independent alternatives for one step
new_alternatives(
  node(
    overrides = "step-combine",
    alternative(
      id = "step-arithmetic-mean",
      decision = "use an arithmetic mean",
      rationale = "the old method"
    ),
    alternative(
      id = "step-weighted-mean",
      decision = "use a weighted mean, weighting each dimension by its variance",
      rationale = "down-weights noisier dimensions"
    )
  ),
  branch = "multi"
)

# single-branch: one coordinated change across two steps
new_alternatives(
  node(
    overrides = "step-scaling",
    alternative(
      id = "step-scaling-zscore",
      decision = "z-score each variable",
      rationale = "puts variables on a common, interpretable scale"
    )
  ),
  node(
    overrides = "step-education",
    alternative(
      id = "step-education-max",
      decision = "use the max of the two school variables",
      rationale = "captures the higher-attainment measure"
    )
  ),
  branch = "single"
)

}
